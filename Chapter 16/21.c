#include<stdio.h>
#include<stdlib.h>
#include<time.h>
#include<ctype.h>
#include<string.h>

enum {NUL, SON, STX, ETX}; // 0 1 2 3 
enum {VT = 11, FF, CR}; // 11 12 13
enum {so = 14, SI, DLE, CAN = 24, EM}; // 14 15 16 24 25
enum {ENQ = 45, ACK, BEL, LF = 37, ETB, ESC}; // 45 46 47 37 38 39

int main( void )
{
	
	return 0;
}

